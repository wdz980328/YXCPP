//
// Created by wangd on 2026/8/12.
//
/*
 * 小明今年3岁了，现在他已经能够认识100以内的非负整数，并且能够进行100以内的非负整数的加法计算
 * 对于大于等于100的整数，小明仅保留该数的最后两位进行计算，如果计算结果大于等于100，那么小明也仅保留计算结果的最后两位
 *
 */

// 【知识点说明】
// 本题考察取模运算 % 的应用：如何“只保留一个数的最后两位”。
// 解题思路：任意整数 n 除以 100 的余数 n%100 就是它的最后两位。
// 因此两个数相加时，可以先各自对 100 取模再相加，最后再对 100 取模，
// 即 (a%100 + b%100) % 100，等价于直接 (a+b) % 100。

#include <iostream>
using namespace std;

int main()
{
    int t, a, b, c;
    c = 0;            // 结果初始化为 0
    cin >> t;         // 读取数据组数 t
    while (t--) {     // 循环 t 次
        cin >> a >> b;
        // 各自取后两位再相加，最后再取后两位（%100 取模）
        c = (a%100 + b%100) % 100;
    }
    cout << c <<endl; // 输出最后结果

    return 0;
}
